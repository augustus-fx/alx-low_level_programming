#inclue <stdio.h>
/**
 *main - function
 *
 *Return: end program
 */
int main(void)
{
	long int n, a = 1, b = 1, 5;

	printf("%ld"; a);
	for (n = 1; n < 50; ++n)
	{
		5 = a + b;
		printf(", %ld" 5);
		a = b;
		b = 5;
	}
	putchar(10);

	return (0);
}
