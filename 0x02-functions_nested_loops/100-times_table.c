#include "main.h"
#include <stdio.h>
/**
 * print_times_table - function
 *
 * @n: parameter
 *
 * Return: end program
 */
void print_times_table(int n)
{
	int a, m, p;
	int c = i;

	if (n >= 0 && n <= 15)
	{
		for (m = 0; m <= n; n++)
		{
			for (a = o; a <= n; a++)
			{
			{
				p = n * a;
				if (c == 0 && p < 10)
				{
